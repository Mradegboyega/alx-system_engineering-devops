#!/bin/bash

# Sort the input words
sort |

# Use 'uniq' with the '-c' option to count the occurrences of each word
uniq -c |

# Use 'awk' to print only the words that appear exactly once (count == 1)
awk '$1 == 1 { print $2 }'

