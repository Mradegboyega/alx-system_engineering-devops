#!/bin/bash

# Read input from the file or standard input
while IFS= read -r line; do
    # Extract the first character of each line and append it to the decoded message
    decoded_message+="${line:0:1}"
done

# Print the decoded message followed by a newline
echo -e "$decoded_message\n"

