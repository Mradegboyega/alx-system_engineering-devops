#!/bin/bash

# Use the 'find' command to locate all .js files in the current directory and its subfolders
# and then delete them using 'rm'.
find . -type f -name "*.js" -exec rm -f {} \;

# Display a message to confirm the action
echo "Deleted all .js files in the current directory and its subfolders."

